version: 2

sources:
  - name: original
    database: raw
    tables:
      - name: comment_count
        freshness: # default freshness
          warn_after: {count: 12, period: hour}
          error_after: {count: 24, period: hour}
        loaded_at_field: file_date
      - name: video_info
        description: This will use the freshness defined above
      - name: view_count
        description: This will use the freshness defined above